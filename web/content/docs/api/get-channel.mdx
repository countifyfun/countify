---
title: Get a counting channel
description: Get information about a specific counting channel
full: true
method: GET
route: /guilds/{guildId}/channels/{channelId}
---

import { Root, API, APIInfo, APIExample, Responses, Response, ResponseTypes, ExampleResponse, TypeScriptResponse, Property, ObjectCollapsible, Requests, Request, APIPlayground } from "fumadocs-openapi/ui";

<Root baseUrl={"https://api.countify.fun"}>

<API>

<APIInfo method={"GET"} route={"/guilds/{guildId}/channels/{channelId}"}>

<APIPlayground authorization={undefined} method={"GET"} route={"/guilds/{guildId}/channels/{channelId}"} bodyType={"json"} path={[{"name":"guildId","type":"string","defaultValue":"","isRequired":true},{"name":"channelId","type":"string","defaultValue":"","isRequired":true}]} query={[]} header={[]} body={undefined} schemas={{}}>

</APIPlayground>

## Path Parameters

<Property name={"guildId"} type={"string"} required={true} deprecated={false}>

</Property>

<Property name={"channelId"} type={"string"} required={true} deprecated={false}>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Retrive a counting channel |
| `404` | Channel or guild not found |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X GET "https://api.countify.fun/guilds/string/channels/string"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.countify.fun/guilds/string/channels/string", {
  method: "GET"
});
```

</Request>

</Requests>

<Responses items={["200","404"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "id": "string",
  "name": "string",
  "count": 0,
  "lastUserId": "string",
  "guild": {
    "id": "string",
    "name": "string"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  id: string;
  name: string;
  count: number;
  lastUserId: string;
  guild: {
    id: string;
    name: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

</Root>